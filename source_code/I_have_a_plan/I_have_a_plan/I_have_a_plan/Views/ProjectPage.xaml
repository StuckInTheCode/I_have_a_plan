<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:I_have_a_plan.Views"
             x:Class="I_have_a_plan.Views.ProjectPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout x:Name="friendStack">
                <Label Text="{Binding Name}" FontSize="Medium" />
                <Label Text="{Binding Deadline}" FontSize="Medium" />
                <Label Text="{Binding Beginning}" FontSize="Medium" />
                <Label Text="{Binding Info}" FontSize="Small" />
               
            </StackLayout>
            <StackLayout x:Name="taskStack">
                <ListView x:Name = "projectList" ItemsSource="{Binding Tasks}" HasUnevenRows="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <views:CustomCell x:Name="view" Title ="Task">
                                <views:CustomCell.HeaderView>
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Name}" />
                                        <Button Clicked="Button_Clicked"/>
                                        <views:ProgressIndicator Value="3" MaxValue="10"/>
                                    </StackLayout>
                                </views:CustomCell.HeaderView>
                                <views:CustomCell.MainView>
                                    <StackLayout Orientation="Vertical">
                                        <Label Text="{Binding Name}" />
                                        <Label Text="{Binding PercentageFinished}" />
                                        <ProgressBar Progress="0.2"/>
                                    </StackLayout>
                                </views:CustomCell.MainView>
                            </views:CustomCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>